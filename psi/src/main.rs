fn main() {
    println!(
        "psi {} — Milestone 1 ships the harness library (psi-core).",
        env!("CARGO_PKG_VERSION")
    );
    println!("See docs/design.md; the TUI arrives in Milestone 4.");
}
